apiVersion: v1
kind: ConfigMap
metadata:
  name: ausf-config
  namespace: open5gs
data:
  ausf.yaml.in: |
    logger:
      file:
        path: /var/log/open5gs/ausf.log
      level: info

    ausf:
      sbi:
        server:
          - address: ${POD_IP}
            port: 7777
        client:
          nrf:
            - uri: http://${NRF_IP}:7777
      serving:
        - plmn_id:
            mcc: "999"
            mnc: "70"
  run.sh: |
    #!/bin/sh
    set -eu
    : "${POD_IP:?POD_IP is required}"
    NRF_IP="$(getent ahostsv4 nrf-svc | awk 'NR==1 {print $1}')"
    sed "s/\${POD_IP}/${POD_IP}/g; s/\${NRF_IP}/${NRF_IP}/g" /etc/open5gs/ausf.yaml.in > /tmp/ausf.yaml
    exec open5gs-ausfd -c /tmp/ausf.yaml
